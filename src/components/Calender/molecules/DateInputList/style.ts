import styled from "@emotion/styled"

export const DetaInputListWrapper = styled.div`
    display: flex;
    flex-direction: column;
    gap: 15px;
`
export const DetaInputWrapper = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 15px;
`